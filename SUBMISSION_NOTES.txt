Render:
Build = pip install -r requirements.txt
Start = python app.py
Secret = GROQ_API_KEY

Demo:
Analyze → Plan → Practice → Submit → Assess & Adapt → Replan → Research → Progress → Ask

Two models:
Qwen 3.8 27B = routine learner tasks
GPT-OSS 120B = compact deep assessment
