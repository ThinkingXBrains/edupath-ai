Render:
Build = pip install -r requirements.txt
Start = python app.py
Secret = GROQ_API_KEY

Demo order:
Analyze Profile → Generate Plan → Generate Practice →
Submit Work → Assess & Adapt → Generate Updated Plan →
Research → Progress → Ask EduPath

Assessment:
GPT-OSS 120B is used twice with compact plain-text formats.
No JSON-schema validation is used for the 120B assessment path.
